<UserControl
    x:Class="Pr0gramm.UI.Controls.PrivateMessage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pr0gramm.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="PrivateMessageXaml">

    <Grid x:Name="MarginGrid">
        <Border Background="{x:Bind Background}">
            <local:SelectableRichTextBlock x:Name="TextContent" Foreground="{StaticResource pr0_textColor}" Text="{x:Bind Text}" Margin="8"/>
        </Border>
    </Grid>
</UserControl>
