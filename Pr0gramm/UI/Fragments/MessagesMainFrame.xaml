<Page
    x:Class="Pr0gramm.UI.Fragments.MessagesMainFrame"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pr0gramm"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Pivot Background="{StaticResource pr0_grey}">
        <Pivot.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource pr0_textColor}"/>
            </Style>
            <Style TargetType="PivotItem">
                <Setter Property="Background" Value="{StaticResource pr0_orange}"/>
                <Setter Property="Margin" Value="0"/>
            </Style>
            <Style TargetType="Frame">
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Pivot.Resources>
        <PivotItem>
            <PivotItem.Header>
                <TextBlock Text="Privat Nachrichten"/>
            </PivotItem.Header>
            <Frame x:Name="PNFrame"/>
        </PivotItem>
        <PivotItem>
            <PivotItem.Header>
                <TextBlock Text="Kommentare"/>
            </PivotItem.Header>
            <Frame x:Name="CommentsFrame"/>
        </PivotItem>
    </Pivot>
</Page>